{
  "name": "@acw/agent-core",
  "version": "0.0.0",
  "private": true,
  "description": "Agent prompts, tool definitions, and the agent-run state machine",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc -b",
    "clean": "tsc -b --clean"
  },
  "dependencies": {
    "@acw/shared": "workspace:*",
    "@acw/github-core": "workspace:*"
  }
}
