{
  "name": "@acw/shared",
  "version": "0.0.0",
  "private": true,
  "description": "Shared TypeScript types, DTOs, Zod schemas, enums, and constants",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc -b",
    "clean": "tsc -b --clean"
  },
  "dependencies": {
    "zod": "4.4.3"
  }
}
